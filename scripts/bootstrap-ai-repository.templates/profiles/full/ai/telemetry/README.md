# Local agent telemetry

Record metadata rather than prompt or source content: task class, risk, route reason, model alias, effort, configuration hashes, skills, tool profile, token/cache counts, tool calls, retries, checks, duration and acceptance. Define retention and access before enabling content capture.

