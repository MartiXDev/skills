# Portable hook implementations

Recommended first capabilities: session snapshot, tool-use guard, tool-output reducer, changed-file checks, completion verification and metadata-only run recording. Give every script a JSON input/output schema, short timeout, fixture tests, reason codes, bounded stdout and documented fail-open/fail-closed behavior.

