# Path-specific Copilot instructions

Add a small `.instructions.md` file only for a genuine file-pattern or module boundary. A local rule should narrow the global contract, not contradict it. Avoid copying root instructions into every path rule.

