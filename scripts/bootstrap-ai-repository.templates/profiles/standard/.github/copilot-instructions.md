# Copilot repository instructions

The authoritative repository guidance is `AGENTS.md`. Use `CONTEXT.md` only as an index and load only the domain documents relevant to the current task. Reusable procedures live in `.agents/skills/`; deterministic checks are configured in `ai/project.json` and executed through `scripts/ai/Invoke-AiChecks.ps1`.

