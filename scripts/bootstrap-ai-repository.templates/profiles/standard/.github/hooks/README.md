# Copilot hook adapters

Register only reviewed lifecycle hooks here. Keep manifests thin and call portable, tested scripts under `scripts/ai/`. Start with audit-only behavior, then warnings, then precise blocking rules after measuring false positives. Never return full build or test logs to the model.

