# Claude Code adapter

Follow the authoritative repository contract in `../AGENTS.md`. Treat `../CONTEXT.md` as a navigation index. Load repeatable procedures from repository skills only when relevant. Prefer deterministic scripts in `../scripts/ai/` for checks and keep model-visible hook output concise.

