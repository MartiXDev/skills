# Codex repository adapter

Codex discovers the root `AGENTS.md` and repository skills. Add `hooks.json` or `config.toml` only after reviewing the current Codex schema and trusting the project. Vendor hook configuration should call portable scripts under `scripts/ai/`; do not duplicate policy in hook manifests.

