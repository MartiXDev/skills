# MartiX Markdown Automation prompts

Prompt assets for this plugin should stay workflow-focused.

Planned prompt types:

- Review remaining Markdown hook findings.
- Decide whether a finding belongs in content, config, or a justified exception.
- Convert repeated findings into `martix-markdown` rule or reference updates.

Do not duplicate the standalone `martix-markdown` rule library here.
