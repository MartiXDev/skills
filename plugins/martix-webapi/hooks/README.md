# MartiX Web API hooks

This plugin does not define active hooks yet.

Future hooks should be opt-in, deterministic, and cheap to run. Candidate hooks:

- Validate OpenAPI or endpoint docs before release.
- Run markdown checks when API docs change.
- Run repository validation before plugin publication.
