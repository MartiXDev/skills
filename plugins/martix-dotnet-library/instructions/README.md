# MartiX .NET Library instructions

Use this plugin-level instruction area for behavior that applies to the whole .NET library workflow.

- Keep reusable .NET guidance in `skills\martix-dotnet-csharp`.
- Keep test-specific guidance in `skills\martix-tunit`.
- Keep validation-library details in `skills\martix-fluentvalidation`.
- Keep docs guidance in `skills\martix-markdown`.
- Use plugin instructions only for bundle-level routing, setup, and release workflow.
